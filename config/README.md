# Config Notes

This directory is reserved for future committed non-secret configuration.

Keep real secrets out of this directory.

Use committed files here for shared desired state, inventories, or examples only when they are intentionally safe to publish inside the repository.
