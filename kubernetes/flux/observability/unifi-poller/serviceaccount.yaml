# The exporter does not need Kubernetes API access, so the workload uses a
# dedicated service account with token mounting disabled.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: unifi-poller
  namespace: observability
automountServiceAccountToken: false
