# Alertmanager and its config sidecars share one pod network namespace. Permit
# only Kubernetes config watches, constrained DNS, and actionable notification
# delivery; ingress remains governed by existing cluster policy.
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: alertmanager-egress
  namespace: observability
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: vmalertmanager
      app.kubernetes.io/instance: metrics-victoria-metrics-k8s-stack
  egress:
    # The operator config-init and config-reloader sidecars watch Kubernetes
    # Secrets. The accepted home-range boundary covers translated API backends.
    - toCIDR:
        - 10.0.0.0/8
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
            - port: "6443"
              protocol: TCP
    # New pods retrieve their first config through the Kubernetes Service VIP.
    # The entity admits that control-plane path even when Cilium cannot infer
    # the translated backend from the workload's initial connection.
    - toEntities:
        - kube-apiserver
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
            - port: "6443"
              protocol: TCP
    # Cilium's DNS proxy learns only the exact notification API names. Both
    # transports are present because truncated responses may retry over TCP.
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s:k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
            - port: "53"
              protocol: TCP
          rules:
            dns:
              - matchName: api.telegram.org
              - matchName: discord.com
    - toFQDNs:
        - matchName: api.telegram.org
        - matchName: discord.com
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
