# This aggregate keeps application-level Flux Kustomizations explicit at the
# cluster root while allowing each workload to declare its own dependencies.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - unifi-kustomization.yaml
