# Flux applies these Cilium custom resources after Cilium and its CRDs exist.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - loadbalancer-ip-pool.yaml
  - bgp-peer-config.yaml
  - bgp-cluster-config.yaml
  - bgp-advertisement.yaml
