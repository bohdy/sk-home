# cloudflared needs no Kubernetes API access; disabling token mounting reduces
# the credential surface of this outbound-only connector.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudflared
  namespace: cloudflare-tunnel
automountServiceAccountToken: false
