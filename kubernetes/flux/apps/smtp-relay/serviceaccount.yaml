# Postfix does not call the Kubernetes API. Keeping a dedicated tokenless
# ServiceAccount makes that boundary explicit and avoids default-token mounts.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: smtp-relay
  namespace: smtp-relay
