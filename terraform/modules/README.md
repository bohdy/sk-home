# Modules

This directory is reserved for reusable Terraform modules shared by multiple stacks.

Create a module here when logic or resources would otherwise be duplicated across stack directories.

Keep module inputs documented, avoid hardcoded secrets, and update this README when shared module conventions change.
